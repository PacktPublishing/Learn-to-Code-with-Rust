The project/directory for this section is called `slices`.

To open the Cargo project in VSCode, go to `File > Open Folder...`. Alternatively, navigate to the project directory in Terminal/PowerShell and execute `code .`.

Locate and select the `slices` directory in the `learn-to-code-with-rust` course folder.

Write your code in the `src/main.rs`.

## Topics Covered in this Section

- String slices
- String literals
- Range extraction syntax
- Range extraction shortcuts
- Slices as Function Parameters
- Array slices
- Mutable slices
